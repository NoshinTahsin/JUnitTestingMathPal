package mathpal;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class TestVolume {

	
	@Test
	void testCone() {
		//fail("Not yet implemented");
		VolumeCalculation calculator = new VolumeCalculation();
		double actualValue = calculator.cone();
		double expectedValue=-1;
		assertEquals(expectedValue, actualValue);
		
	}
	

	
	/*@Test
	void testCube() {
		//fail("Not yet implemented");
		VolumeCalculation calculator = new VolumeCalculation();
		double actualValue = calculator.cube();
		double expectedValue=-1;
		assertEquals(expectedValue, actualValue);
		
	}
	
	@Test
	void testCylinder() {
		//fail("Not yet implemented");
		VolumeCalculation calculator = new VolumeCalculation();
		double actualValue = calculator.cylinder();
		double expectedValue=235.5;
		assertEquals(expectedValue, actualValue);
		
	}
	
	@Test
	void testSphere() {
		//fail("Not yet implemented");
		VolumeCalculation calculator = new VolumeCalculation();
		double actualValue = calculator.sphere();
		double expectedValue=-1;
		assertEquals(expectedValue, actualValue);
		
	}
	
	
	
	
*/
}
